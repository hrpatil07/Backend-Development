var userInput;
var userName;
userInput = 5;
userInput = 'Max';
if (typeof userInput === 'string') {
    userName = userInput;
}
//unknown is more relaible than any , any is you can assign any type to it and it will not give any error.

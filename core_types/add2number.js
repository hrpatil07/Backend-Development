function add2number(var1,var2){
    return (var1 + var2)
}
console.log(add2number('2'+'4'));
//This will show un defined but we need the output as 6 so this is a logical error and can be resolved using typescript
